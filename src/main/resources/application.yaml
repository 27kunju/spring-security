spring:
  application:
    name: security


  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

#  security:
#    user:
#      name: admin
#      password: admin123